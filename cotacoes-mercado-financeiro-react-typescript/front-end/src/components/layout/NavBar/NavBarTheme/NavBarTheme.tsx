import { createTheme, Theme } from "@mui/material";

const themeNavBar: Theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#002332",
    },
  },
});

export default themeNavBar;
