import StocksList from "../layout/StocksList";
import styles from "./Home.module.css";

const Home = () => {
  return (
    <section>
      <StocksList />
    </section>
  );
};

export default Home;
